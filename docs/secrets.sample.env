# Required secrets for this orchestrator repo (configure in GitHub -> Settings -> Secrets)
DEPLOY_HOST=deployer@example.com
DEPLOY_SSH_KEY=-----BEGIN OPENSSH PRIVATE KEY-----\nFAKE_PRIVATE_KEY_LINE\n-----END OPENSSH PRIVATE KEY-----
CERT_EMAIL=ops@example.com

# Optional orchestrator repo secrets
# Map of service name -> env lines. Newlines must be escaped.
SERVICE_ENVS_JSON={"my-app":"DATABASE_URL=postgres://db.example.com:5432/app\nAPI_KEY=supersecret\nNODE_ENV=production"}
# Deploy all services on push ("all"), skip ("none"/"skip"), or comma-separated allowlist. 
PUSH_DEPLOY_SERVICES=all

# Service repository secret (set in each service repo)
# GitHub token with repo scope used by service workflow to trigger this orchestrator.
DEPLOY_ORCHESTRATOR_TOKEN=ghp_example_personal_access_token
